apiVersion: apps/v1
kind: Deployment
metadata:
  creationTimestamp: null
  labels:
    app: annualnet
  name: annualnet
spec:
  replicas: 1
  selector:
    matchLabels:
      app: annualnet
  strategy: {}
  template:
    metadata:
      creationTimestamp: null
      labels:
        app: annualnet
    spec:
      containers:
      - image: rabbitmq-microservices-kubernates_annual-net
        name: rabbitmq-microservices-kubernates-annual-net-v8vjg
        imagePullPolicy: IfNotPresent
        env:
          - name: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE
            valueFrom:
              configMapKeyRef:
                name: eureka-config
                key: eureka-client
